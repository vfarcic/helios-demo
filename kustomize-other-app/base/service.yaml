---

apiVersion: v1
kind: Service
metadata:
  name: other-app
  labels:
    app.kubernetes.io/name: other-app
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app.kubernetes.io/name: other-app
