---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: other-app
  labels:
    app.kubernetes.io/name: other-app
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: other-app
  template:
    metadata:
      labels:
        app.kubernetes.io/name: other-app
    spec:
      containers:
        - name: other-app
          image: docker.io/vfarcic/helios-demo2:latest
          ports:
            - containerPort: 8080
          resources:
            limits:
              cpu: 500m
              memory: 512Mi
            requests:
              cpu: 250m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /
              port: 8080
          readinessProbe:
            httpGet:
              path: /
              port: 8080
